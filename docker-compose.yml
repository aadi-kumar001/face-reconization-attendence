version: "3.8"
services:
  attendance:
    build: .
    ports:
      - "5000:5000"
    devices:
      - "/dev/video0:/dev/video0"   # remove if running headless / API-only
    volumes:
      - ./instance:/app/instance
    env_file:
      - .env
    restart: unless-stopped
