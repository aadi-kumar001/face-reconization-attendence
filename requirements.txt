# Web framework
Flask==3.0.3
PyJWT==2.9.0

# Database ORM
SQLAlchemy==2.0.35

# Computer vision core
opencv-python==4.10.0.84
numpy==1.26.4

# Face recognition backends (install at least one; insightface preferred)
insightface==0.7.3
onnxruntime==1.19.2          # use onnxruntime-gpu instead if you have a CUDA GPU
# face_recognition==1.3.0    # fallback backend; also needs: dlib, cmake

# Fast similarity search at scale (optional but recommended >200 enrolled faces)
faiss-cpu==1.9.0              # use faiss-gpu instead if you have a CUDA GPU

# Dev / testing
pytest==8.3.3
